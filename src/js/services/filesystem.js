/*
    Store and load files:
    * Local storage,
*/

var config = require('../config');

var filetree = {
    filename: 'root',
    files: []
};

function FileSystem () {}


FileSystem.prototype.create = function (filename, write) {
    
};



module.exports = FileSystem;
